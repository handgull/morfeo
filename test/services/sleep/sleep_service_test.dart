import 'package:flutter_test/flutter_test.dart';
import 'package:morfeo/services/sleep_service.dart';

void main() {
  late SleepService service;

  setUp(() {
    service = SleepServiceImpl();
  });

  group('when the method read is called', () {});
}
